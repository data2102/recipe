# CLAUDE.md

작업을 시작하기 전에 **[`docs/claude-code-brief.md`](docs/claude-code-brief.md) 를 읽어라.**
개발 지시서다. 무엇을 어떤 순서로 만드는지가 여기 있다 (8장 작업 순서).
왜 그렇게 정했는지는 **[`docs/v1-spec.md`](docs/v1-spec.md)** 에 있다.
올리는 절차는 **[`docs/deploy.md`](docs/deploy.md)** 에 있다.
다 만든 걸 어떻게 쓰는지는 **[`docs/how-to-use.md`](docs/how-to-use.md)** 에 있다 —
동작을 바꾸면 여기도 같이 고친다.
화면을 새로 그릴 일이 생기면 **[`docs/ui-references.md`](docs/ui-references.md)** 를 먼저 봐라.
다른 앱들이 이미 푼 것과, **우리가 안 가져오기로 한 것**이 이유와 같이 적혀 있다.
이유를 모르고 코드를 고치면 이미 검토해서 버린 선택지로 되돌아가게 된다.

[`README.md`](README.md) 의 "🤖 Claude Code에게" 절에 **반드시 지킬 것 7가지**가 있다.
그게 이 문서의 본문이다 — 여기서 다시 적지 않는다.


## 2026-09-10 UI/UX 구현 변경

사용자의 전체 UI/UX 개선 요청에 따라 홈을 오늘 메뉴 우선으로 바꿨다.
현재 동작은 `docs/how-to-use.md`에 정리했다. 아래 과거 구현 설명 중
다음 내용은 변경되었다.

- 홈·장보기 기본 주는 이번 주이고, 명시적인 `week=next`를 이동 시 유지한다.
- 담기는 즉시 처리하고 날짜는 식단에서 변경한다. `setDay`는 `starts_on`으로 대상을 정한다.
- 레시피 제목은 상세로 바로 이동하고 기록·삭제는 더보기로 분리했다.
- 최근 조리한 메뉴도 추천 후보로 남기며 이미 담은 메뉴는 페이지를 나누기 전에 제외한다.
- 장보기는 합친 목록이 기본이다. 수량은 원문 근거를 보여주며 임의 환산하지 않는다.
- 상시 재고는 만들지 않는다. `shopping_list.excluded`는 해당 날짜의 목록에서 제외한 재료만 저장한다.
- 최근 구매는 CHECK로 표시한다. HAVE는 사용자가 이번 목록에서 보유를 확인한 경우다.
- 새 구매 이벤트의 `source`는 `CHECKOFF:<목록 ID>:<재료 ID>`다. 취소는 해당 이벤트만 삭제한다.
- SQL 또는 이 동작을 바꾸면 `npm run test:ux` 회귀 시나리오를 함께 확인한다.
- 배포 전 새 `20260910000000_list_exclusions.sql` 마이그레이션이 필요하다.


## 2026-09-15 식단을 날짜로

사용자가 쓰면서 넷을 말했다. 아래가 그 결과다 — 자세한 동작은
`docs/how-to-use.md` 에 있다.

- **홈이 추천 화면에서 식단 화면이 됐다** (`app/Plan.tsx`). 이번 주/다음 주
  탭 대신 **열나흘을 세로로** 늘어놓고 날짜마다 담긴 메뉴를 보여준다.
  추천(`suggest`·`weave`)은 지우지 않았다 — 이 화면에서 뺐을 뿐이다.
- **날짜마다 한 줄 메모** (`day_note`, `lib/notes.ts`). "저녁 약속" 처럼
  그날 못 먹는 사정을 적어둔다. 담을 때 날짜를 고르는 창에도 같이 보인다.
- **담기가 날짜를 묻는다** (`app/PlanButton.tsx`, `actions.ts` `planOnDate`).
  고른 날짜가 어느 주인지까지 정한다 (`shopping.whichOf`).
  `addToWeek`·`addToWeekOn`·`setDayOfWeek` 는 `planOnDate` 로 합쳐졌다.
- **장보기 체크가 안 내려가던 것**을 고쳤다 (`app/Shopping.tsx`).
  `useOptimistic` 이 액션이 끝나는 순간 되돌아가서, 신호가 나쁘면 체크가
  풀린 것처럼 보였다. 이제 서버가 따라올 때까지 들고 있고, 저장 중에도
  **누른 줄만** 잠긴다.
- 지운 것: `RecipeList`·`RecipeRow`·`RecipeSheet`·`PickDay`·`WeekStrip`·
  `MealBasket`·`Week`. 줄 안의 "⋯" 에 있던 **다른 날에 만들었어요**와
  **레시피 삭제**는 레시피 상세의 **더보기**로 옮겼다 (`recipe/[id]/More.tsx`).
- 배포 전 새 `20260915000000_day_note.sql` 마이그레이션이 필요하다.

---

## 자주 쓰는 명령

```bash
python tools/build_dictionary_seed.py            # CSV -> db/seed_dictionary.sql
python tools/build_dictionary_seed.py --check    # 검증만 (파일 안 씀)
python tools/verify_seed.py                      # 스키마+시드를 SQLite 에 올려 확인
python tools/verify_pipeline.py                  # 파이프라인 한 바퀴 (API 키 불필요)

python tools/build_prompts.py                    # prompts/*.md -> web/lib/parse/prompts.ts
python tools/build_migrations.py                 # db/*.sql -> supabase/migrations/
python tools/build_migrations.py --check         # 어긋났는지만 확인
python tools/verify_migration.py                 # 마이그레이션을 진짜 PostgreSQL 에

cd web && npm run dev                            # 앱 (http://localhost:3000)
cd web && PARSER_FAKE=1 npm run dev              # LLM 없이 /add 화면만 돌려보기
cd web && npm run lint && npm run build          # CI 가 보는 것
psql "$DATABASE_URL" -f db/seed_dev.sql          # 손으로 돌려볼 예시 데이터

python -m pipeline ingest 캡처.png               # 수집 → 파싱 → 정규화 → 저장
python -m pipeline show 1
python -m pipeline unmapped                      # 사전에 없어서 못 붙인 표기

pip install -r requirements.txt                  # accuracy_test.py 용
python tools/accuracy_test.py --compare          # 1패스 vs 2패스 (API 키 필요)
```

`tools/` 의 시드·마이그레이션·검증 스크립트는 표준 라이브러리만 쓴다.
의존성을 추가하지 마라 — API 키 없이 CI 에서 돌아가는 게 이것들의 조건이다
(PostgreSQL 은 `psql` 로 붙는다. psycopg 를 받지 않는다). 파이프라인 검증은
LLM 자리에 가짜 응답을 넣어 배관만 잰다 (파서 정확도는 accuracy_test.py 몫).

`verify_seed.py` 와 `verify_migration.py` 는 둘 다 필요하다. 앞은 SQLite 라
어디서나 돌아가고 schema.sql 의 치환 규칙을 검증하지만, `BIGSERIAL`·`FILTER`·
`LATERAL` 같은 PostgreSQL 문법은 거기서 안 걸린다. 뒤가 그 구멍을 메운다.

`pipeline/` 의 3층을 한 덩어리로 합치지 마라. 파서는 원문을 옮기기만 하고
표준화는 코드가 사전을 보고 한다. 합치면 사전이 좋아질 때마다 재파싱해야 한다.

---

## 건드릴 때 주의

| 파일 | 주의 |
|---|---|
| `db/seed_dictionary.sql` | **자동 생성물.** 직접 고치지 말고 CSV 를 고친 뒤 재생성한다. `verify_seed.py` 가 어긋남을 잡는다 |
| `data/ingredient-dictionary.csv` | **실측 기록.** 추론으로 행을 채우지 마라. 문서에 있다는 이유로 넣지도 마라 |
| `db/schema.sql` | PostgreSQL 기준. SQLite 치환 규칙이 머리말에 있고 `verify_seed.py` 가 그 규칙을 쓴다. 끝의 "핵심 쿼리 3개" 주석은 `verify_migration.py` 가 실제로 파싱시킨다 — 컬럼을 바꾸면 같이 고쳐라 |
| `supabase/migrations/*.sql` | **올린 것은 얼린다** (`build_migrations.py` 의 FROZEN 에 이름+해시). 스키마를 바꾸려면 `db/schema.sql` 을 고치고 **델타 파일을 손으로 새로 쓴다.** 얼린 파일은 원본과 대조하지 않는다 — 갈라짐은 `verify_migration.py` 가 두 DB 를 만들어 컬럼으로 잰다 |
| `web/app/similar/` | **언젠가 지울 화면.** 저장할 때 막기 시작하면 새로 안 쌓인다 (`web/app/import/` 와 같은 성격) |
| `web/app/yeobaek/*.css` | **복사본.** 여백 디자인 시스템 원본 저장소를 고치고 다시 복사한다 (`web/app/yeobaek/README.md`) |
| `web/lib/db.ts` | 서버 전용. Client Component 에서 import 하면 접속 문자열이 번들에 실린다 |
| `db/seed_dev.sql` | **예시 데이터.** 마이그레이션이 아니다. 실제 DB 에 넣지 마라 |
| `prompts/*.md` | **파서의 본체.** 고치면 `PARSER_VERSION` 을 올리고 정확도를 다시 재라 |
| `web/lib/parse/prompts.ts` | **자동 생성물.** `prompts/` 를 고친 뒤 `build_prompts.py` 로 재생성 |
| `web/lib/parse/normalize.ts` | `pipeline/normalize.py` 와 **같은 규칙이다.** 한쪽만 고치지 마라 |

---

## 현재 상태

지시서 8장 작업 순서 기준.

- **1~3번 완료.** 프로젝트 셋업 + 마이그레이션, 재료 사전 시드,
  레시피 목록 3탭 + 만들었어요(날짜 선택)
- **4번 캡처 → 2패스 파싱 → 확인 화면 → 저장 — 완료.** `/add` 에서
  캡처 1장이 레시피가 되는 걸 브라우저로 확인했다 (가짜 응답으로 배관 검증).
  **실제 캡처로는 아직 안 재봤다** — API 키가 있어야 한다
- **5번 미분류 확인 — 미뤘다.** 실제 레시피를 넣어봐야 안다.
  통과 기준은 미분류 10% 안쪽 (`SELECT * FROM unmapped_term ORDER BY hit_count DESC`).
  지시서는 절반을 넘으면 6번으로 넘어가지 말라고 하는데, 아직 안 재봤다 —
  사용자가 알고 미룬 것이다
- **6번 이번 주 담기 + 장보기 3단 분류 — 완료.** 탭 3 에서 담고, 담은
  요리의 재료가 합쳐져 BUY/CHECK/HAVE 로 갈린다. 체크하면 구매 기록이 생긴다
- **9번 PWA + Web Share Target — 완료.** 설치하면 공유 시트에 뜬다.
  `/share` 가 받아서 원본만 먼저 보관하고 `/add` 로 넘긴다
- **7번 링크 파싱 + 캡처 폴백 — 완료.** robots.txt 를 보고 읽는다.
  못 읽으면 캡처로 안내하고, 제목만 건지면 "이름만 저장" 을 낸다
- **8번 냉장고 재료 가중치 — 완료.** 칩은 주소(`?have=`)에만 산다.
  필터가 아니라 가중치라 하나도 안 맞아도 목록이 안 빈다

**지시서 8장 9단계를 다 돌았다.** 남은 건 실제로 써보는 것이다 —
판정 기준은 "4주 뒤 마트에서 실제로 열었는가" 하나다.

### 앱 쪽 규칙

- **한 앱이 화면 폭에 맞춰 바뀐다. PC 판을 따로 만들지 마라.**
  900px 부터 넓어지고(960px), 탭바가 위로 올라가고, 화면이 두 칸으로
  갈린다 (`globals.css` 의 `.board` · `.wide`). 폰에서는 아무것도 안 바뀐다.
  **폰에서는 보고 PC 에서는 정리한다** — 붙여넣기(Ctrl+V)·끌어놓기는 PC 에만
  있는 길이라 안내도 큰 화면에서만 낸다
- **화면은 셋이다 — 식단 · 메뉴 고르기 · 장보기** (`app/TabBar.tsx`). 한 화면에
  다 넣었더니 폰에서 2,200px 짜리 한 장이 돼서, 마트에서 쓰는 장보기까지
  여섯 번을 밀어야 했다. 새 기능을 넣을 때 **어느 축의 일인지 먼저 정한다**
- **식단(`/`)은 보는 자리고 메뉴 고르기(`/recipes`)는 고르는 자리다.**
  예전에는 홈이 추천 화면이었다 (오늘의 제안 + 다른 메뉴 + 접힌 이번 주
  식단). 고르는 일과 "뭘 먹기로 했더라" 가 한 화면에 겹쳐서 자주 하는
  뒤쪽이 아래로 밀렸다. 식단에 추천을 다시 올리지 마라 — `lib/recipes.ts`
  의 `suggest`·`weave` 는 그대로 있고, 되살릴 자리는 고르는 쪽이다
- **"집에 있어요" 는 장보기에서만 쓴다.** 거기가 답을 쓰는 자리고
  (`shopping_list.excluded`), 식단은 그 값을 **읽기만** 해서 펼친 재료에
  "집에 있음" 을 적는다. 쓰는 자리를 두 화면에 두지 마라 — 같은 걸 두
  군데서 고치면 어긋난다
- **서버 액션은 `revalidatePath("/", "layout")` 으로 턴다.** 담기 하나가
  식단과 장보기를 같이 바꾸고, 체크 하나가 장보기와 식단을 같이 바꾼다.
  한 곳만 새로 그리면 나머지가 어제 걸 보여준다

- **DB 는 생 SQL 로 직접 붙는다** (`web/lib/db.ts`). Supabase REST 를 안 쓰는
  이유는 그 파일 머리말에 있다 — 쪼개면 `schema.sql` 과 다른 로직이 두 벌 생긴다
- **브라우저는 DB 에 안 붙는다.** 조회는 Server Component, 변경은 Server Action
- **색을 하드코딩하지 마라.** 여백 토큰(`var(--...)`)만 쓴다.
  파랑(`--accent`)은 누를 수 있는 것에만 — "68일" 같은 정보는 텍스트 3단계로
- **모양은 `.ds-*` 를 먼저 찾아 쓴다.** 카드·버튼·폼·탭·칩·체크박스·배지·배너는
  전부 여백에 있다 (`web/app/yeobaek/README.md`). CSS 모듈에는 배치만 남긴다 —
  같은 모양을 모듈에서 다시 그리면 디자인 시스템이 좋아져도 앱은 안 바뀐다.
  시스템 기본값을 덮어쓸 일이 있으면 `globals.css` 아래쪽 한 군데에 모은다
- **탭 2 의 오래된 순 정렬을 뒤집지 마라.** 그 정렬이 곧 추천이다.
  이름순(`?sort=name`)은 **찾을 때 쓰는 것**이고 기본이 아니다 —
  기본을 이름순으로 바꾸면 추천이 통째로 사라진다
- **추천은 돌린다 — 걸러내지 않는다** (`recipes.ts` `suggest`). "다른 거
  볼래요"(`?again=N`)는 다음 묶음을 보여주고 끝에서 처음으로 돌아온다.
  "당분간 싫어요" 를 DB 에 적지 마라: 언제까지 싫은지는 사람도 모르고,
  기한을 지어내면 그게 다시 틀린 추천이 된다. 영영 싫으면 지운다
- **두 갈래를 각자 자기 풀에서 돌리지 마라** (`recipes.ts` `weave`).
  "오랜만에" 후보는 보통 몇 개뿐이라 (만든 적 있고 + 30일) 따로 돌리면
  위쪽 세 줄이 눌러도 그대로다 — 실제로 "계속 똑같은 게 반복" 됐다.
  한 줄로 엮어 5개씩 자른다: 한 갈래가 바닥나면 나머지가 그 자리를
  채우고, **한 바퀴 도는 동안 같은 요리가 두 번 안 나온다.** 제자리에서
  돌리면(0·1·2 → 3·4·0) 넘길 때마다 방금 본 게 딸려 온다
- **담을 때 날짜를 묻는다 — 하지만 필수는 아니다** (`app/PlanButton.tsx`,
  `actions.ts` `planOnDate`). 고르는 사람 머릿속에는 이미 "목요일에 이거"
  가 있는데 받아줄 데가 없어서 식단에 가서 또 골라야 했다. 그래도
  `day_of_week` 는 여전히 NULL 을 허용한다 — "날짜는 나중에" 가 있어야
  담기가 안 무거워진다. 고르는 창에는 **그날 이미 담긴 메뉴와 적어둔
  약속**을 같이 낸다. 빈 날을 찾으려고 여는 창이다
- **지난 날은 물어보되 자동으로 기록하지 마라.** 정해둔 날짜가 지났는데
  그날 조리 기록이 없으면 식단에서 물어본다. 약속이 생겨 건너뛴 날이 흔한데
  자동으로 체크하면 안 만든 게 만든 것으로 남고, `last_cooked_on` 하나가
  30일 추천과 탭 2 정렬을 통째로 틀어놓는다. "안 먹었어요" 는 요일만 미정으로
  되돌린다 — 못 먹었을 뿐 이번 주에서 빼는 건 아니다
- **그날의 메모는 날짜에 붙는다 — 주가 아니라** (`lib/notes.ts`, `day_note`).
  약속이 있는 날은 **안 정해도 되는 날**이라고 적어두는 자리다. 앱이
  해석하지 마라 — 메모가 있다고 담기를 막거나 추천에서 빼지 마라. 사람이
  보고 사람이 정한다. 빈 메모는 행을 남기지 않는다 ("약속 없음" 과 "아직
  안 적었다" 를 구별할 이유가 없다). 저녁·점심 칸으로 나누지 마라 —
  v1 은 저녁 한 끼를 정하는 앱이고, 칸을 늘리면 안 채운 칸이 먼저 생긴다
- **날짜를 적을 때는 한국 기준으로 적는다** (`lib/say.ts` 의 `TZ`). 서버는
  UTC 라 그냥 두면 한국 새벽 0~9시에 "오늘" 이 어제가 된다. "며칠 지났나"
  같은 비교는 CURRENT_DATE 로 둔다 — 몇 시간으로 30일 문턱이 바뀌지 않는다
- **구매 기록이 생기는 자리는 장보기 체크 하나뿐이다** (`shopping.toggle`).
  `purchase` 에 INSERT 하는 코드가 앱 전체에 거기 하나다 — "N일 전에
  샀어요" 가 이상하면 그 체크를 의심해라. **날짜는 한국 기준으로 적는다**:
  `CURRENT_DATE` 로 적으면 한국 새벽 0~9시에 체크한 게 어제 산 것으로 남고,
  하루 차이가 그대로 "어제 샀어요" 라는 문장이 된다
- **식단에서 펼친 재료의 체크는 구매 기록을 만들지 않는다.** 집에 있다는
  건 오늘 샀다는 뜻이 아니다. 없는 날짜를 지어내면 다음 주에 "3일 전에
  샀어요" 라는 거짓말이 나온다. 구매 기록은 마트에서 장보기 체크로만 생긴다
- **레시피가 사라지는 자리는 `recipes.remove` 하나뿐이다.** "별로였어요"
  (레시피 줄)와 "지울게요"(닮은 것끼리)가 둘 다 거기로 간다. 예전에는
  `status='BAD'` 로 숨기기만 했는데 쓰는 사람이 지우기로 정했다. 되돌릴 수
  없어서 화면이 한 번 더 묻는다. `shopping_list_recipe` 만 CASCADE 가 없으니
  (지난 주 기록이 요리 하나 지웠다고 사라지면 안 된다) **손으로 먼저 떼야
  삭제가 된다** — 그 순서가 `remove` 안에 있다
- **닮은 레시피는 판정하지 말고 보여준다** (`lib/similar.ts`). 이름은
  글자 2-gram, 재료는 `raw_name` 집합으로 각각 자카드를 재고 **둘 중 큰
  쪽**을 쓴다 — 이름이 전혀 다른 같은 요리(감바스/버터갈릭새우)는 재료로만,
  재료를 아직 안 붙인 것은 이름으로만 잡힌다. **`/similar` 의 문턱(30%)은
  임시다** — 실제로 몇 개가 걸리는지 보고 정하려고 낮게 잡아둔 것이고,
  저장 확인 화면에 붙일 값은 그걸 보고 정한다. 자동 병합은 만들지 마라:
  같은 이름이어도 다른 레시피일 수 있다
- **짝이 아니라 묶음으로 낸다** (`similar.ts` 의 union-find). 짝으로
  늘어놓으면 닮은 게 4개일 때 카드가 6장이고 김치찌개가 다섯 장에 나온다 —
  실제로 "같은 게 반복된다" 는 말이 나왔다. A~B, B~C 면 A·B·C 를 한 장에 낸다
- **재료 겹침만으로는 계열을 못 가른다.** 제육볶음과 돼지고기 두루치기는
  돼지고기·양파·대파·간장·설탕을 다섯 가지나 같이 쓴다 — 개수·비율·문턱을
  어떻게 만져도 안 갈린다 (실제로 두루치기가 제육볶음 묶음에 들어갔고
  문턱을 올려도 안 빠졌다). 그래서 **겹친 것 중에 드문 재료가 하나는
  있어야** 재료를 근거로 센다 (`rareLimit`): 양념은 사전의
  `category='양념'` 으로 아예 빼고, 남은 것 중 전체의 1/4 넘게 나오는 건
  바탕 재료로 본다. 감바스/버터갈릭새우는 새우·페페론치노로 걸리고,
  제육볶음끼리는 이름으로 걸린다. IDF 무게(`weighted`)는 그 위에 얹는다
- **문턱을 코드에 박지 마라** (`LEVELS`, `?min=`). 화면에서 30·45·60% 를
  고른다. 기본은 제일 넉넉한 쪽이다 — 훑어보는 화면이라 **놓치는 쪽이 더
  나쁘다.** 아닌 게 섞이면 눈으로 넘기지만 안 나온 중복은 찾을 길이 없다.
  화면에는 가중치가 아니라 **겹친 개수**를 적고, 겹쳤는데도 근거로 안 센
  경우는 "겹치지만 바탕 재료예요" 라고 그대로 말한다 (0% 만 적으면 거짓말로 읽힌다)
- **저장한 레시피는 `/recipe/[id]?edit=1` 에서 고친다.** 고칠 때도 **사전
  대조를 다시 한다** — 이름이 바뀌면 붙는 재료가 달라지고 장보기 합산도
  달라진다. 화면이 보낸 ingredient_id 를 믿지 마라. 재료 행은 통째로 갈아
  끼운다 (장보기는 이름으로, 식단은 볼 때마다 읽어서 행 id 를 붙들지 않는다).
  조리 기록·사진·원본은 건드리지 않는다 — 원본이 남아야 재파싱할 수 있다
- **만든 사진은 조리 기록에 붙는다** (`cook_log.photo_key`). 레시피에 따로
  매달지 마라 — 언제 만든 건지 모르는 사진만 쌓인다. 그래서 한 번 만들 때
  한 장이다. 사진을 올리면 오늘 만든 기록이 생긴다(없을 때). **지난 요일
  자동 체크와 다르다** — 사람이 사진을 고르는 행동이 앞에 있고, 버튼 글자가
  그렇게 될 거라고 미리 말한다
- **보관함은 비공개다. 사진은 `/photo/<조리기록 id>` 로 내보낸다.**
  저장 경로를 주소에 넣지 마라 — 버킷 구조가 밖으로 샌다.
  폰 사진은 3~5MB 라 브라우저에서 1600px JPEG 으로 줄여 올린다
- **만드는 법은 `/recipe/[id]` 에서 읽는다.** 목록은 제목과 재료 몇 개만
  보여준다. 캡처로 넣은 레시피는 원본 링크가 없어서, 이 화면이 없으면
  만드는 법을 저장해두고도 못 읽는다. **타이머·단계 넘기기·화면 켜두기는
  넣지 마라** — 요리 중 화면 UX 는 아직 안 정한 것이다 (지시서 9장)
- **배포가 바뀌면 열어둔 화면의 서버 함수가 사라진다** ("Server Action ...
  was not found"). 저장 실패와 다르다 — 다시 눌러도 계속 막힌다. 그때는
  보관해둔 원본으로 다시 시작하는 길을 낸다 (`/add?shared=1&assets=…`)
- **같은 초안을 두 번 저장하지 않는다.** `save()` 가 `source_asset.recipe_id`
  를 `FOR UPDATE` 로 잡고 이미 붙어 있으면 그 id 를 돌려준다. 화면에서 버튼을
  막는 것만으로는 못 막는다 — 폰이 잠기면 서버는 저장을 끝냈는데 응답만
  사라지고, 사용자 눈에는 실패라 다시 누른다 (실제로 두 건이 생겼다)
- **오래 걸리는 일은 시작할 때 화면을 바꾼다.** `<form action={...}>` 안에서
  `setState` 를 하면 React 가 뒤따르는 트랜지션과 묶어버려서 **끝날 때까지
  화면이 안 바뀐다.** `onSubmit` 으로 받아 긴급 갱신으로 그린다
  (`app/add/Add.tsx`). 30초 동안 아무 반응이 없으면 사용자는 또 누른다
- **레시피 저장은 왕복 8번이다.** 재료마다 INSERT 를 보내면 15개짜리가 왕복
  30번이고, 함수와 DB 가 먼 대륙에 있으면 그것만으로 몇 초다. 배열로 한 번에
  넣는다 (`lib/parse/store.ts`). 함수 지역은 `web/vercel.json` 에서 DB 옆으로
- **원본은 파싱보다 먼저 보관한다.** 파싱이 실패해도 `source_asset` 에 남아야
  재파싱할 수 있다 (원칙 ⑤). `app/add/actions.ts` 의 순서를 바꾸지 마라
- **확인 화면에서 물어보는 문장에 물음표를 달지 마라.** 아래에 넣을지 말지를
  고르는 버튼이 붙어서, 질문이면 "아니요"가 재료를 통째로 뺀다 (원칙 ②)
- **쓰는 사람은 둘이다.** 무료 요금제 안에서 돈다 — 서버리스에서는 Supabase
  Transaction pooler(6543)로 붙고 인스턴스당 접속은 1개다
- **어느 주인지는 날짜가 정한다 — 상태가 정하지 않는다.**
  `shopping_list.starts_on` 이 그 주의 월요일(한국 기준)이고, 오늘이 속한
  월요일이 곧 이번 주다 (`shopping.weekStart` — DB 를 안 보는 순수 함수).
  예전에는 OPEN 이 이번 주, NEXT 가 다음 주였는데 **장보기 끝을 안 누르면
  주가 안 넘어갔다** — 9월 8일에 8/31~9/6 이 이번 주로 보였다. 주를 사람
  손에 맡기지 마라. `status` 는 이제 "장을 다 봤나"(OPEN/DONE)만 말한다
- **"장보기 끝" 은 주를 옮기지 않는다** (`shopping.finish`). 그 주 장을 다
  봤다는 표시일 뿐이고, 월요일이 오면 안 눌러도 이번 주가 바뀐다.
  되돌리기(`weeks.reopen`)도 상태 한 줄이면 된다 — 승격이라는 게 없어졌다
- 셋은 없다. 다다음 주까지 짜는 사람은 없고, 늘어날수록 "이번 주" 가
  흐려진다. 담기·요일 옮기기·장보기 체크는 화면이 보고 있는
  주(`?week=next`)에 걸리고, 폼마다 그 값을 실어 보낸다
- **식단은 주를 안 고른다 — 열나흘을 쭉 늘어놓는다** (`lib/week.ts` `horizon`,
  `app/Plan.tsx`). 이번 주 월요일부터 다음 주 일요일까지다. 예전에는 두
  화면이 "이번 주 / 다음 주" 탭으로 갈려 있었는데, 사람이 묻는 건 "이번
  주에 뭐 담았지" 가 아니라 **"수요일에 뭐 먹지"** 라서 날짜 하나 보려고
  탭을 오갔다. 주 탭을 다시 만들지 마라
- **주가 남아 있는 건 장보기뿐이다** (`?week=`, 탭바도 장보기에만 물려준다).
  장은 주에 한 번 보니까 목록이 주 단위다. **담을 때 어느 주인지는 날짜가
  정한다** (`shopping.whichOf`) — 화면이 보낸 주를 믿지 마라. 화면이 열려
  있는 동안 자정이 지나면 그 값은 틀린다
- **요일은 저장하고 날짜는 계산한다.** DB 에는 `day_of_week` (0~6) 만 있고,
  며칠인지는 **그 주의 월요일에 그냥 더한다** (`lib/week.ts` `plan`).
  `planned_on` 을 행마다 컬럼으로 만들지 마라: 요일과 날짜 두 벌이 어긋난다.
  주 단위 시작일(`starts_on`)은 컬럼이 맞다 — 그건 주마다 하나뿐이다
- **요일만 적지 마라 — 날짜를 같이 적는다.** "이번 주 화요일" 이 며칠인지
  화면에 없으면 사람이 다시 센다 ("이번주 다음주로 하니까 실제로 몇일껀지
  더 헷갈려"). 날짜 말은 `lib/say.ts` 한 군데에 있다 (`dateSay`·`dateTiny`·
  `dateFull`·`dateRange`) — 화면마다 `toLocaleDateString` 을 부르지 마라
- **끝낸 장보기를 지우지 마라.** 목록 하나가 지난 한 주다 (`lib/weeks.ts`).
  "장보기 끝" 은 이번 주를 통째로 닫는 일이라 되돌릴 길이 있어야 한다 —
  담았던 요리도 요일도 그대로 두고 상태만 바꾼다. 되돌리기는 **열린 목록이
  없을 때만** 낸다. 두 주가 동시에 열리면 "이번 주" 가 뭔지 알 수 없다
- **장보기 SQL 은 `db/schema.sql` 의 "핵심 쿼리 3개" 중 (3)번과 같은 것이다**
  (`web/lib/shopping.ts`). 한쪽만 고치면 갈라진다 — `verify_migration.py` 가
  스키마 쪽을 실제로 파싱시키니 거기서 안 걸리는 차이가 생긴다
- **장보기를 요리별로 봐도 항목은 한 벌이다** (`app/ShoppingByRecipe.tsx`).
  요리별로 따로 세면 대파가 세 요리에 들어갈 때 세 단을 산다. 합친 목록의
  이름(label)으로 상태를 읽어서, 한 군데서 체크하면 다 체크된다. 겹치는
  재료에는 "다른 요리에도" 라고 적어둔다
- **장보기의 체크와 식단의 체크는 다른 것이다.** 생김새가 비슷하지만
  장보기는 누르는 칸(구매 기록이 생긴다), 식단은 잠긴 칸(읽기 전용)이다.
  둘을 같은 컴포넌트로 합치지 마라
- **이미 체크한 장보기 항목의 칸은 바꾸지 마라.** 체크하면 구매 기록이 생겨서
  다시 재면 HAVE 로 옮겨가는데, 마트에서 칸이 바뀌면 어디까지 샀는지 놓친다
- **체크한 항목은 맨 아래로 내려가고, 접지 않는다** (`app/Shopping.tsx`).
  접어두면 누른 게 사라져 보여서 한 번 더 누른다
- **장보기 체크는 서버가 따라올 때까지 체크된 채로 둔다.** `useOptimistic`
  은 액션이 끝나는 순간 원래 값으로 돌아가고 화면이 새로 그려져야 다시
  체크가 된다 — 마트 신호가 나쁘면 그 사이가 벌어져서 **체크가 도로 풀린
  것처럼 보였다** (DB 에는 들어가 있는데 앱을 다시 켜야 "구매했어요" 에
  있었다). 소원을 들고 있다가 서버가 같은 값을 보내면 그때 놓는다
- **저장 중이라고 목록 전체를 잠그지 마라 — 누른 줄만 잠근다.** 마트에서는
  연달아 집는다. 전부 잠그면 그 사이의 탭이 통째로 버려진다
- **장보기 한 줄은 이름 하나다.** 사전에 붙은 '대파' 와 못 붙인 '대파' 는
  DB 에서 다른 행이라 (`NEED_SQL`) 그냥 내면 두 줄이 되고 두 단을 산다.
  행은 그대로 두고 화면에 낼 때 합친다 — **사전에 붙은 쪽을 남긴다**
  (체크가 구매 기록으로 이어져야 한다). 칸은 덜 확신하는 쪽으로
- **서버 액션 본문은 1MB 가 기본이다. 캡처 한 장이 그걸 넘는다.** 넘으면
  파싱 실패가 아니라 **413 으로 화면이 통째로 죽는다** ("A server error
  occurred"). 폰에서 줄여 보내는 게 진짜 해결이고 (`web/lib/frames.ts`),
  `next.config.ts` 의 `bodySizeLimit` 은 안전망이다 — 서버리스 요청 본문에도
  한도가 있어서 (Vercel 4.5MB) 거기만 키워봐야 그 앞에서 막힌다
- **서버 액션이 죽어도 화면은 살려라.** 413 처럼 요청 자체가 거절되면 우리
  코드까지 오지도 못해서 평소 오류 통로를 안 탄다 — `await ingest(...)` 를
  try/catch 로 감싸지 않으면 페이지가 통째로 죽는다. 사용자 눈에는 앱이
  고장 난 것으로 보이고, 뭘 잘못했는지도 알 수 없다
- **긴 캡처는 줄이지 말고 자른다.** 스크롤 캡처를 한 장으로 줄이면 글자가
  뭉개져서 못 읽는다. 폭은 두고 세로로 자른다 — 어차피 파서는 여러 장을
  한 번에 읽는다. 자른 자리에서 글자가 반 토막 나지 않게 조금 겹친다.
  보통 폰 캡처는 자르지 않는다 (한 장이면 될 걸 나누면 값만 두 배다)
- **영상은 브라우저에서 장면만 뽑아 보낸다** (`web/lib/frames.ts`). 서버로
  영상을 올리지 마라 — 수십 MB 라 올리다 실패하고, 서버에서 뽑으려면
  ffmpeg 이 필요하다. 플랫폼에서 영상을 내려받지도 마라 (지시서 4장):
  사용자가 자기 폰에서 녹화한 파일만 읽는다. 고르게 훑되 거의 같은 장면은
  버린다 — 멈춰 있는 구간이 길어서 안 그러면 같은 그림만 여러 장 간다
- **`/share` 에서 파싱하지 마라.** 원본만 보관하고 `/add` 로 넘긴다. 파싱은
  30초쯤 걸려서, 공유를 누른 사람을 빈 화면 앞에 세우면 다시 안 쓴다
- **대신 `/add` 가 알아서 시작한다.** 공유 시트에서 이 앱을 고른 것 자체가
  "정리해줘" 다 — 넘어와서 버튼을 한 번 더 누르게 하면 공유가 지름길이 아니게
  된다. 첫 화면부터 "읽는 중" 으로 그린다 (`autoStart` 를 `useState` 초기값에
  쓴다 — effect 뒤에 바꾸면 폼이 한 번 번쩍인다). **인스타 링크만 온 경우는
  시작하지 않는다** — 링크로 못 읽는 걸 이미 아는데 8초를 태우고 실패를
  보여줄 이유가 없다. 실패하면 공유받은 글과 주소를 그대로 둔 폼으로 돌아간다
- **`public/sw.js` 로 화면을 캐시하지 마라.** 설치 가능 조건을 채우려고 둔
  파일이다. 목록·장보기는 서버가 그때그때 그린다 — 캐시하면 어제 걸 본다
- **링크로 가져오기는 지금 화면에서 빠져 있다.** 읽히는 곳이 적어서 캡처·
  붙여넣기만으로 먼저 써보기로 했다 (사용자가 정한 것이다). 서버 쪽은
  그대로 남아 있다 (`ingestLink` · `lib/parse/link.ts`) — 되살릴 때 화면만
  붙이면 된다. **주소는 계속 들고 간다** — 공유받은 원문 주소는 화면에
  안 보여도 `source_url` 로 저장돼야 한다 (저작권, 지시서 4장)
- **유튜브는 공식 API 로 설명란을 먼저 본다** (`YOUTUBE_API_KEY`, 없으면
  예전처럼 "제목만"). 페이지를 긁어봐야 제목뿐이라 API 가 먼저다.
  **자막은 안 건드린다** — 공식 경로는 영상 주인만 되고 비공식 경로는
  지시서 4장이 금지한 것이다
- **링크를 읽기 전에 robots.txt 를 본다** (`web/lib/parse/link.ts`).
  막힌 곳은 안 읽고 캡처로 안내한다. 이 가드를 우회하지 마라 (지시서 4장)
- **칩은 장보기에서 빼는 도구다 — 추천이 아니다.** "집에 있는 걸로 만들 수
  있어요" 는 화면에서 뺐다 (`lib/fridge.ts` `weighted` 는 남아 있다). 간장
  하나만 눌러도 간장 들어가는 레시피가 전부 올라와서, 추천이 아니라 목록을
  한 벌 더 낸 것에 가까웠다 — 양념은 어디에나 들어간다. 되살리려면 "흔한
  재료" 를 가르는 기준부터 정해야 한다
- **냉장고 재료를 DB 에 저장하지 마라.** 주소에만 실린다 (`?have=` 는 사전에
  붙은 id, `?haveRaw=` 는 레시피에 적힌 표기). 상시 재고를 만들면 갱신을 안
  해서 어긋나고, 어긋나는 순간 추천이 쓸모없어진다
- **칩은 이번 주에 담은 요리 기준이다.** 추천 목록까지 넣으면 아직 먹기로
  정하지도 않은 요리의 재료까지 나와서 수십 개가 된다. 담은 것 기준이면
  장보기 범위와 같아진다 — 칩이 묻는 건 "살 것 중에 뭐가 집에 있나" 다
- **칩은 사전이 아니라 레시피 기준이다.** 사전은 40종인데 레시피 표기는 훨씬
  많다. 사전에 붙은 것만 칩으로 내면 대부분을 "집에 있어요" 라고 말할 수가
  없어서 전부 사야 할 것으로 남는다. 이름도 레시피에 적힌 그대로 쓴다
  (원칙 ①) — '진간장' 으로 붙었어도 내 레시피가 '간장' 이면 칩은 '간장' 이다
- **`pg` 는 BIGINT 를 문자열로 준다.** `lib/db.ts` 에서 숫자로 바꿔 받는다 —
  안 그러면 `{ id: number }` 라고 적어둔 타입이 전부 거짓말이 된다

### 폰 앱 (`native/`) — 화면을 다 옮겼다

**웹이 본체다.** 앱은 같은 서버를 보는 다른 화면이다.
**웹을 앱 때문에 고치지 마라** — 판정 기준("4주 뒤 마트에서 실제로
열었는가")이 아직 안 나왔다.

| 화면 | 앱 | |
|---|---|---|
| 식단 | `app/index.tsx` | 열나흘 · 메모 · 지난 날 물어보기 |
| 메뉴 고르기 | `app/recipes.tsx` | 검색 · 정렬 · 재료 칩 |
| 장보기 | `app/shopping.tsx` | 3단 분류 · 매대 · 되돌리기 |
| 레시피 상세 | `app/recipe/[id].tsx` | 재료 · 만드는 법 · 사진 · 지우기 |
| 지난 주 | `app/weeks.tsx` | 토글로 펼치면 식단과 같은 모양 |
| 레시피 넣기 | `app/add.tsx` | 캡처 → 파싱 → 확인 → 저장 |
| 담기 판 | `components/PlanSheet.tsx` | 넷이 같은 판 하나를 쓴다 |

**일부러 안 옮긴 것** (없는 걸 있는 척하지 않고 화면에 그렇게 적었다):

- **영상에서 장면 뽑기** — `web/lib/frames.ts` 는 video + canvas 라
  다시 써야 한다 (층 표의 "웹" 이 이것 하나다). 캡처는 `expo-image-manipulator`
  로 같은 값(긴 변 1280 · JPEG 0.7)까지 줄여 보낸다
- **긴 캡처 자르기** — 스크롤 캡처를 한 장으로 줄이면 글자가 뭉개져서
  세로로 잘라야 하는데, 그건 안 옮겼다. 보통 폰 캡처는 한 장이다
- **레시피 고치기** (`?edit=1`) — 고칠 때 **사전 대조를 다시 해야** 해서
  화면만 옮겨서 될 일이 아니다
- **닮은 것끼리**(`/similar`) · **가져오기**(`/import`) — 위 표에 적힌 대로
  **언젠가 지울 화면**이다. 지울 것을 두 벌로 만들지 않는다
- **공유 시트**(`/share`) — PWA 의 Web Share Target 이다. 안드로이드
  share intent 는 다른 물건이라 옮기는 게 아니라 새로 내는 일이다
- **유튜브에서 찾기**(`/youtube`) — 서버가 공식 API 키를 쓴다.
  화면은 쉬운데 아직 안 했다

- **순수 층을 복사하지 않는다 — 그 파일을 그대로 읽는다**
  (`native/metro.config.js` 의 `watchFolders`). 복사하면 `verify_layers.py`
  는 통과하면서 두 벌이 되고, 날짜 규칙 하나가 웹과 앱에서 다르게 돈다
- **창구는 `native/lib/pure.ts` 하나다.** 앱의 다른 파일은 `web/lib/` 을
  직접 import 하지 않는다. 어느 파일이 순수인지 아는 곳이 한 군데여야
  실수도 한 군데서만 난다 — `verify_layers.py` 가 이걸 잰다
- **`db.ts` 가 딸려 들어가면 접속 문자열이 앱 파일에 실린다.**
  TypeScript 는 이걸 안 막는다 (`web/node_modules` 에서 `pg` 타입을
  찾아내고 통과시킨다 — 실제로 해봤다). CI 가 **번들을 grep 해서** 잰다
- **타입 검사만으로는 모자라다.** Metro 는 tsc 와 다른 규칙으로 파일을
  찾는다. CI 가 안드로이드 번들을 실제로 묶어본다
- **토큰은 번들에 박힌다** (`EXPO_PUBLIC_`). 비밀이 아니라 자물쇠다 —
  새면 서버에서 바꾸고 앱을 다시 올린다. DB 접속 문자열·API 키는 절대
  안 싣는다 (`native/.env.example`)
- **여백 토큰을 `native/lib/tokens.ts` 에 베껴 적었다.** RN 은 CSS 변수를
  못 읽는다. 색을 화면에 하드코딩하지 않는다는 규칙은 그대로고, 여기가
  그 "한 군데" 다 — **원본(`web/app/yeobaek/tokens.css`)이 바뀌면 같이 바꾼다**
- **장보기 규칙은 웹과 같은 것이다.** 칸(BUY/CHECK/HAVE) 안에서 매대로
  나눈다 — 칸을 없애고 매대로만 늘어놓으면 3단 분류가 사라진다.
  체크는 서버가 따라올 때까지 들고 있고, 저장 중에도 누른 줄만 잠그고,
  체크한 것은 맨 아래로 내려가되 접지 않는다
- 아이콘 세트를 들이지 마라. 탭바는 웹처럼 글자다 — 한쪽만 아이콘으로
  바꾸면 생김새가 갈린다
- **탭이 아닌 화면은 `href: null` 로 숨긴다** (`app/_layout.tsx`).
  `app/` 에 파일을 두면 expo-router 가 경로로 만드는데, 그게 곧 탭이
  되면 **화면은 셋**이라는 규칙이 조용히 깨진다. 같이 쓰는 컴포넌트는
  `components/` 에 둔다 — `app/` 에 두면 그것도 경로가 된다
- **화면을 다시 읽는 자리는 `useFocusEffect` 다.** 웹의
  `revalidatePath("/", "layout")` 자리다 — 담기 하나가 식단과 장보기를
  같이 바꾸는데, 한 번만 읽으면 탭을 오갈 때 어제 걸 보여준다
- **확인 화면에서 근거를 지어내지 마라.** 왜 물어보는지는 서버가
  `evidence` 에 적어 보낸다. 화면이 한 줄 더 쓰면 같은 말이 두 번
  나온다 (실제로 "택1" 이 두 줄로 나왔다)

### 언젠가 네이티브로 갈 때를 위해 — 경계 하나만 지킨다

**지금 옮기지 않는다.** 판정 기준("4주 뒤 마트에서 실제로 열었는가")이 아직
안 나왔고, 안 쓰는 앱을 두 벌 만드는 건 제일 비싼 실수다. 대신 **언제든 갈
수 있는 상태**만 유지한다. 그 상태라는 게 딱 하나다 — `tools/verify_layers.py`
가 매 푸시마다 잰다.

| 층 | 무엇 | 네이티브로 가면 |
|---|---|---|
| **순수** | `say.ts` · `recipe-sort.ts` · `*.types.ts` · `youtube-evidence.ts` | **그대로 옮겨진다.** 날짜 셈·정렬·모양이 여기 있다 |
| **서버** | `db.ts` 를 타는 전부 (`shopping`·`week`·`recipes`·`parse/*` …) | **API 뒤로 들어간다.** 화면을 다시 그려도 **SQL 은 한 벌로 남는다** |
| **웹** | `frames.ts` (video + canvas) | **다시 써야 한다** |

- **순수 층에 `db`·`react`·`next`·DOM 전역을 끌어들이지 마라.** 한 다리
  건너 들어오는 것도 막는다 (순수는 순수만 import 한다)
- **`web/lib/` 에 파일을 새로 만들면 `verify_layers.py` 의 셋 중 하나에 적어라.**
  안 적으면 CI 가 막는다 — 그게 이 검사의 진짜 목적이다. "이건 어느 층인가"
  를 만들 때 한 번 묻게 만든다
- 이건 네이티브와 상관없이도 이미 지키던 규칙이다 (`*.types.ts` 를 따로
  뺀 이유가 그것이다 — 서버 코드가 브라우저 번들에 실리면 안 된다).
  네이티브는 그 규칙에 이유를 하나 더 얹을 뿐이다
- **RN 은 폰에서 DB 에 직접 못 붙는다** (접속 문자열이 기기에 실린다).
  `db/policy.sql` 이 RLS 를 켜고 정책을 0개로 둬서 REST 문도 닫아놨다 —
  로그인이 없다는 결정과 한 몸이다. 그래서 **API 층을 먼저** 낸다
  (`web/app/api/`, 2026-09-16 시작)
- **API 는 토큰 없이 안 연다** (`lib/api/guard.ts`). 로그인이 없어도
  안전했던 이유는 **서버만 DB 에 붙기 때문**인데, HTTP 문을 열면 그
  전제가 깨진다 — 주소만 아는 누구나 읽고 지운다. `APP_API_TOKEN` 이
  없으면 **API 전체가 503 으로 거부한다** (fail closed): 설정을 깜빡한
  배포가 조용히 열려 있는 것보다 시끄럽게 막히는 쪽이 낫다.
  웹 화면은 이 문을 안 쓰므로 토큰이 없어도 앱은 멀쩡히 돈다
- **API 와 웹 화면이 로직을 두 벌로 갖지 않는다.** 둘 다 `lib/` 의 같은
  함수를 부른다 — 화면만 다르고 SQL 은 하나다. 새 엔드포인트를 만들 때
  거기서 쿼리를 새로 쓰지 마라
- **날것의 DB 오류를 밖으로 흘리지 마라.** 없는 레시피 id 는 외래키에서
  걸리는데, 제약 이름(`..._fkey`)은 앱에 쓸모가 없고 스키마만 샌다.
  요청이 틀린 것이면 400 으로 사람 말을 돌려준다
- **경로를 새로 만들면 첫 두 줄이 `allow(request)` 다.** 깜빡하면 그 문
  하나가 통째로 열리고 아무도 모른다. `verify_ux.ts` 가 `app/api/` 의
  `route.ts` 를 전부 읽어서 문지기를 부르는지 센다 — 안 부르면 CI 가 막는다
- **로직을 서버 액션 안에 두지 마라** (`app/actions.ts`). 네이티브는 서버
  액션을 못 쓰고 `app/api/` 를 타는데, 하는 일이 액션 안에 있으면 저쪽에
  한 벌을 더 쓰게 된다. 액션은 폼을 값으로 바꿔 `lib/` 에 넘기고
  `revalidatePath` 를 부르는 자리다 — 그래서 `markCooked` 의 본체를
  `recipes.cooked` 로 뺐다
- **주를 앱이 정하는 곳은 장보기뿐이다.** 장보기는 주 단위로 보는 화면이라
  "다음 주 것도 미리" 가 있어야 한다 (`GET /api/shopping?week=next`).
  **담기는 다르다** — 날짜가 주를 정한다 (`whichOf`). 앱이 켜져 있는 동안
  자정이 지나면 앱이 보낸 주는 틀린다
- **정렬·검색은 서버에 만들지 마라** (`GET /api/recipes`). 목록을 통째로
  주고 폰이 가른다. 그 정렬 코드(`lib/recipe-sort.ts`)는 순수 층이라
  네이티브로 그대로 옮겨진다 — `?sort=` 를 만들면 같은 규칙이 두 벌이 된다

### 이미 내린 결정 — 되돌리지 마라

**문서에 있는 별칭을 사전에 채우지 않는다.** `docs/v1-spec.md` 7장의 변형 표에는
있지만 CSV 에 없는 표기가 있다 (간마늘, 조선간장, 미림, 신김치, 묵은지).
맞추지 않기로 정했다 — 어떤 표기가 실제로 들어올지는 레시피를 넣어봐야 알고,
문서의 예시를 근거로 사전을 부풀리면 실측 기록이 추측으로 오염된다.

사전을 키우는 경로는 하나다: **레시피를 저장하다 `unmapped_term` 에 쌓인 걸
사람이 보고 CSV 에 넣는다.** 스펙 7장 "미분류 처리"가 그 설계다.
미분류가 장보기에 개별 항목으로 나가 중복 구매가 생기는 건 감수하는 오류다.

### 열려 있는 결정

**스택은 정해졌다** (지시서 7장 권장안 — Next.js + Supabase, 안드로이드 PWA).
남은 건 지시서 9장의 목록이다: 중복 레시피 병합 기준, 무료 한도·구독가,
요리 중 화면 UX, 검색·정렬 옵션, 사진 교체 흐름.

**정하라고 재촉하지 마라.** 지금 정하면 근거 없이 정하게 된다.
